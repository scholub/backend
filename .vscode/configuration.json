{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run from Workspace Root",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
