# AUTOGENERATED FROM 'queries/paper/get_cache.edgeql' WITH:
#     $ gel-py --dir queries --no-skip-pydantic-validation


from __future__ import annotations
import dataclasses
import datetime
import gel
import uuid


@dataclasses.dataclass
class GetCacheResult:
    id: uuid.UUID
    paper_id: str
    modified: datetime.datetime


async def get_cache(
    executor: gel.AsyncIOExecutor,
    *,
    paper_id: str,
) -> GetCacheResult | None:
    return await executor.query_single(
        """\
        select Paper::Cache {
          paper_id,
          modified
        } filter .paper_id = <str>$paper_id limit 1;\
        """,
        paper_id=paper_id,
    )
